// Exporta todos los modelos de la carpeta models

const Server = require('./server');
const Usuario = require('./usuario');
const Categoria = require('./categoria');
const Documento = require('./documento');




module.exports = {
    Server,
    Usuario,
    Categoria,
    Documento
}

